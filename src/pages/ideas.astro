---
import Base from '../layouts/Base.astro';

/**
 * giscus config — comments/suggestions powered by GitHub Discussions.
 *
 * To activate (takes ~5 minutes):
 *  1. Push this repo to GitHub (public) and enable Discussions in repo Settings.
 *  2. Install the giscus app: https://github.com/apps/giscus
 *  3. Open https://giscus.app, pick your repo + an "Ideas" category,
 *     then copy `repoId` and `categoryId` from the generated snippet here.
 */
const GISCUS = {
  repo: 'your-github-username/tech-blog', // TODO
  repoId: '', // TODO
  category: 'Ideas',
  categoryId: '', // TODO
};

const configured = GISCUS.repoId !== '' && GISCUS.categoryId !== '';
---

<Base
  title="Suggest an idea · Nick's Tech Blog"
  description="Tell me what I should build or write about next."
>
  <h1>What should I build next? 🤔</h1>
  <p>
    Sometimes I genuinely don't know what to work on. So instead of guessing,
    I'm asking you: <strong>what should I build or write about next?</strong>
  </p>
  <p>
    Drop your idea below, or 👍 the ones you'd like to see. The most upvoted
    suggestion becomes my next project.
  </p>
  <p lang="vi" class="vi-note">
    🇻🇳 Bạn cứ góp ý bằng tiếng Việt thoải mái nhé — mình đọc hết!
  </p>

  {
    configured ? (
      <script
        is:inline
        src="https://giscus.app/client.js"
        data-repo={GISCUS.repo}
        data-repo-id={GISCUS.repoId}
        data-category={GISCUS.category}
        data-category-id={GISCUS.categoryId}
        data-mapping="specific"
        data-term="blog-ideas"
        data-strict="0"
        data-reactions-enabled="1"
        data-emit-metadata="0"
        data-input-position="top"
        data-theme="preferred_color_scheme"
        data-lang="en"
        crossorigin="anonymous"
        async
      />
    ) : (
      <div class="setup-note">
        <p>
          <strong>Suggestions box coming soon.</strong> (Owner: follow the
          giscus setup steps in <code>src/pages/ideas.astro</code> to enable
          it.)
        </p>
      </div>
    )
  }
</Base>

<style>
  .vi-note { color: var(--muted); }
  .setup-note {
    border: 1px dashed var(--border);
    border-radius: 8px;
    padding: 0.25rem 1rem;
    color: var(--muted);
  }
</style>
